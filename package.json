{
  "name": "y",
  "version": "y",
  "description": "my resume repo",
  "scripts": {
    "generate": "md-to-pdf resume.md --config-file ./config/format.json --launch-options '{ \"args\": [\"--no-sandbox\"] }'",
    "generate:jp": "md-to-pdf resume.jp.md --config-file ./config/format.json --launch-options '{ \"args\": [\"--no-sandbox\"] }'"
  },
  "repository": "git@github.com:yk-jp/resume.git",
  "author": "yk-jp <k82.y12@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "md-to-pdf": "^5.2.4"
  }
}
